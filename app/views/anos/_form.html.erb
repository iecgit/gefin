<%= form_for(@ano) do |f| %>
  <% if @ano.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ano.errors.count, "error") %> prohibited this ano from being saved:</h2>

      <ul>
      <% @ano.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ano %><br />
    <%= f.number_field :ano %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
