<%= form_for(@ativo_circulante_operacional) do |f| %>
<% if @ativo_circulante_operacional.errors.any? %>
<div id="error_explanation">
	<h2><%= pluralize(@ativo_circulante_operacional.errors.count, "error") %> prohibited this ativo_circulante_operacional from being saved:</h2>
	<ul>
		<% @ativo_circulante_operacional.errors.full_messages.each do |msg| %>
		<li>
			<%= msg %>
		</li>
		<% end %>
	</ul>
</div>
<% end %>
<div class="field">
	<%= f.label :descricao %>
	<br />
	<%= f.text_field :descricao %>
</div>
<div class="field">
	<%= f.label :valor %>
	<br />
	<%= f.text_field :valor %>
</div>
<div class="field">
	<%= f.label :analise_financeira %>
	<br />
	<%= f.collection_select(:analise_financeira_id, AnaliseFinanceira.all, :id, :empresaAno) %>
</div>
<div class="actions">
	<%= f.submit %>
</div>
<% end %> 